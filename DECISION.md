# Decisions

## Scaffold purpose

- This repository is the Hard Eng scaffold, added to existing projects so they can follow Hard Eng principles. Setup affects only the target repository.
- The scaffold checks for Hard Eng changes at agent session start and updates its installed setup under the update rules below.
- The scaffold adds gates for the entire project based on its actual tech stack. Other flows will be defined later.
- The scaffold sets up codebase-memory-mcp and the Context Mode plugin for Claude, Codex and Copilot using each client's supported project-local configuration.
- The scaffold prepends its `AGENTS.md` instructions to the project's `AGENTS.md`. Updates replace the previously added Hard Eng section, keeping it once at the top and preserving the project's own content below.

## Implementation checklist

Unchecked boxes are agreed requirements awaiting implementation and meaningful verification. Tick a box only after its required behavior is implemented and verified in the applicable environment. Previous implementation claims and CI runs do not prove this rebuild works.

Implement the remaining agreed changes continuously, keeping each change minimal and verifying it before marking it complete. The user authorized completing the checklist without further per-item approval on 2026-09-09. This does not authorize committing or pushing this rebuild, global installation, modifying other projects or changing remote branch protection. The automatic update-commit behavior below is a requirement for the installed scaffold.

- [ ] Scaffold setup = install instructions, skills, stack-appropriate gates and agent integrations; activate enforcement at agent completion, Git pre-push and CI as part of setup.
- [ ] Project coverage = inspect the entire target project and configure gates for its actual supported tech stacks; execution scope follows the check-scope rules below.
- [x] Project instructions = prepend Hard Eng's instructions to the target AGENTS.md; on updates, replace that section once at the top and preserve the project's content below.
- [x] Existing configuration = preserve project configuration and local scaffold edits; combine settings where they can safely coexist, and ask before resolving conflicts. Do not silently overwrite existing skills, MCP configuration, gates or hooks.
- [ ] YAGNI enforcement = review actual changes against the user's request at completion; identify unsupported additions and remove them; automated scope/quality checks support this review but cannot certify semantic necessity by themselves.
- [ ] Addition rule = name the current agreed requirement and why existing code/direct commands are insufficient before adding machinery; leave it out otherwise. Tests must prove a required outcome or meaningful failure; optional expansion needs an explicit request. No extra justification files or enforcement framework.
- [ ] Gates = repository-owned `hard-eng.gates.json` declares applicable check commands.
- [x] Gate command execution = run declared native commands in their package or project directory; nonzero exits, missing executables and timeouts fail. Reject empty or malformed configuration before execution; report each command's result without losing earlier failures.
- [x] Template command compatibility = Python, JavaScript/TypeScript and Dart/Flutter templates declare the executable and arguments together in each command list, matching the native runner; preserve existing checks and report settings.
- [x] Dependency setup order = run every declared `lockfiles` command before other checks, regardless of its position in the configuration. Stop with failure if dependency setup fails, is unavailable or times out; do not run checks against an unprepared project.
- [x] Parallel gate execution = run explicitly independent (`parallel: true`) checks with at most two in flight, refill a free slot without waiting for a slower sibling, and keep unmarked checks as serial barriers. Dependency setup runs first and alone. Reject invalid flags and colliding declared report files before execution; preserve individual timeouts, grouped child output and every failure. Templates opt read-only checks in; arbitrary test scripts, generators, builds and heavy scanners stay serial until independence and native worker usage are reviewed.
- [ ] Gate result validation = findings, crashes and incomplete scans fail; never swallow failures or report an unrun check as passed. Verify scanner-specific results.
- [x] Fallow result validation = require a fresh combined dead-code/duplication report; fail on reported findings, analysis diagnostics, missing required sections/counters, invalid scope and reported skipped work. Preserve native command failures. Reuse the existing Fallow report declaration and shared report preparation.
- [x] OSV result validation = require a fresh supported report with scanned package identities; fail on reported vulnerabilities/groups, scan errors/findings, missing or malformed results and zero scanned packages. Preserve native failures. Setup copies the scanner readers and preserves conflicting local edits; missing reader code fails verification.
- [x] Semgrep result validation = require fresh JSON with `--time`, nonempty executed rules and scanned source content; reject findings, errors, reported skipped rules/files, analysis timeouts, malformed/missing scope and native failures. Safe/unsafe native examples prove the reader for Python, JS/TS and Dart; production rule selection, complete scan coverage and Dart rule installation remain separate unchecked requirements.
- [x] Gitleaks result validation = require fresh native SARIF 2.1.0 with one Gitleaks run, configured rule identities and no findings. File/history checks use separate redacted reports; reject missing/stale/malformed output and preserve native failures, including findings behind exit zero. SARIF does not enumerate scanned files/commits or all omissions; complete scan scope and exception enforcement remain unverified.
- [x] Hard Eng skill routing = keep SKILL.md as a short router; move substantive workflow, setup, gate and efficiency instructions into linked references. Preserve the existing requirements, resolve local reference links, distribute those files through the existing installer and preserve conflicting local edits.
- [ ] Test quality = assert independently expected observable outcomes and relevant failure cases; regression tests must catch the original defect; merely executing code or checking mock calls does not prove the required behavior; coverage alone does not certify test quality.
- [ ] Runtime validation = no separate Zod/schema-library gate or mandatory dependency; test relevant valid/invalid external inputs through the existing test gate using the project's validation code.
- [ ] Scanner policy = all findings must be resolved, including pre-existing findings; no existing-debt baseline exemption.
- [ ] Git pre-push = enforce full applicable gates against the actual commits being pushed; failed gates or broken checks block the push while ordinary editing remains available. An uncommitted fix must not make committed code appear to pass.
- [x] Pre-push launcher = install an executable launcher that invokes the shared runner and fails when that runner is missing. Replace the recognized Hard Eng shortcut, preserve unrelated hooks, and keep repeat installation unchanged. Check logic remains in the shared runner.
- [ ] Remote enforcement = setup adds the CI workflow; CI independently runs applicable gates under the check-scope rules below. Missing/skipped expected checks fail the aggregate gate.
- [ ] Branch protection = ask for separate approval before changing remote branch rules. When approved, require the `hard-eng` check on protected `main`, including for administrators, with force-push and deletion disabled.
- [ ] Simplicity = no custom hash checks, fingerprint inventories or gate-result caching framework; use normal Git operations and the tools' native behavior.
- [ ] Tool versions = latest on every gate run; tool updates occur independently of Hard Eng updates.
- [ ] Check scope = full checks for affected packages + dependents + shared checks; uncertain impact → all supported packages.
- [ ] Scaffold-only updates = when an installed update changes only Hard Eng scaffold files, run relevant scaffold checks without running the target project's application checks or application CI. Changes affecting project code or project configuration run applicable project checks; mixed changes do not receive the scaffold-only exemption.
- [ ] Research skill = rebuild `skills/research` with codebase, comparison, external evidence, library/API and troubleshooting routes; derive coverage before recommendations; distinguish documented/tested/inferred/unknown claims; explain remaining gaps before claiming sufficiency; no old receipt scripts or mandatory paperwork.
- [ ] Python gates = Ruff format/lint/complexity + Pyrefly + pytest + Vulture + jscpd + deptry + conditional Import Linter; validate speed/compatibility on the first real repository.
- [ ] Python typing = enforce Pyrefly `preset = "strict"` + `check-unannotated-defs = true` across the applicable package scope; no broad ignores, disabled strict checks or Any substitutions to silence findings; existing narrow exception policy applies.
- [x] Python typing defaults = install strict Pyrefly settings when absent, preserve conflicting settings, check unannotated definitions, and explicitly run Ruff annotation/Any-import checks over the template's `src` and `tests` paths. Safe local inference remains allowed; full project scope and overrides still require verification.
- [x] JavaScript/TypeScript typing lint = explicitly reject `var`, explicit `any` and uninitialized implicit-any declarations with native Biome rules while allowing safe inference.
- [x] Strict typing configuration = require native typing gates, enforce TypeScript's strict options and JavaScript checking, and validate Dart strict inference/casts/raw types plus dynamic/missing-type lints. Setup retains project typecheck commands and unrelated Dart settings; runtime rejects relaxed settings, missing typing roles, omitted declared TypeScript sources, and Dart exclusions/ignored diagnostics, including nested and included configuration. Safe inference is allowed. Dart configuration parsing requires PyYAML.
- [x] Selected native performance/async checks = batch Ruff RUF017 and ASYNC210 into the existing Python annotation command, Biome noAccumulatingSpread into the existing JS/TS explicit-rule command, and Dart unawaited_futures into the existing analyzer settings/check. Preserve other project checks and intentional Dart unawaited work. Reject targeted patterns and tested configuration-off cases without adding a scanner process or dependency; broader performance budgets and source-scope verification remain separate.
- [ ] Python complexity = explicitly enable Ruff C901 + PLR0912 + PLR0915; configure maximum complexity 10, branches 12 and statements 50 per function using Ruff's documented defaults; preserve stricter existing limits; existing narrow exception policy applies; preview-only nesting checks are not enabled by default.
- [x] Python complexity command = the template adds a native Ruff check with explicit 10/12/50 limits, retaining the existing lint check for stricter project thresholds. Ruff 0.16.4 boundary fixtures accepted the limits and rejected excess complexity, branches and statements; relaxed settings could not raise the ceilings and stricter settings still failed through lint. Installation included the command, installer preservation/repeat tests passed, and the real runner rejected an excessive function despite a later passing command in an isolated application package. No dependency or source file was added.

Complexity acceptance remains open: the installed root-package scan reported 13 findings in Hard Eng's copied runner/report reader. These were not suppressed. The source repository's full runner also remains unavailable without `hard-eng.gates.json`. The old template-compatibility fixture rejects the changed template against its historical snapshot; current installation and focused native command execution were verified separately, not reported as that fixture passing. Existing-project gate configuration is preserved, so adapting it and reviewing exclusions/exceptions remain necessary. Native references: [Ruff limits](https://docs.astral.sh/ruff/settings/), [configuration precedence](https://docs.astral.sh/ruff/configuration/).

- [x] Hard Eng complexity repair = resolved the 13 runner/report findings and three installer findings without suppressions or relaxed limits. Split command preparation, report validation and installer stages into focused functions; moved unchanged document/configuration checks into `.hooks/gate_config.py` so handwritten files remain below 700 lines. Setup copies the module and missing imports fail clearly. Ruff lint, complexity and formatting passed across all four source files. Native scanner/image, strict typing, parallel execution, document, LCOV and real pytest-cov checks passed; actual local pre-push passed with required committed modules and rejected a missing report module despite an uncommitted repair. This supersedes the source-complexity failure above; complete project gate activation and scope/exception review remain pending.
- [ ] Python dependency declarations = deptry for supported dependency definitions; check missing/unused dependencies, direct use of transitive dependencies, production use of development-only dependencies and standard-library packages declared as dependencies; account narrowly for proven dynamic/plugin usage; missing or incomplete expected analysis fails.
- [x] Deptry result validation = native deptry 0.25.1 accepted a clean project and rejected DEP001–DEP005 fixtures. Added its JSON report to the Python template and reused the existing report/log validation flow. The reader rejects findings, malformed/missing/stale reports, absent/zero-source diagnostics and skipped-source warnings, including the reproduced zero-exit mixed clean/malformed source scan. Native failures and missing executables remain failures despite a later pass. Tests used deptry with installed fixture dependency metadata in an isolated uv tool environment; no global tool installation or new runtime dependency was added. Full dependency-definition, environment, exclusion/ignore and dynamic-import coverage remain unverified. Native contracts: [rules](https://deptry.com/rules-violations/), [environment and configuration](https://deptry.com/usage/), [JSON report](https://github.com/osprey-oss/deptry/blob/0.25.1/python/deptry/reporters/json.py), [scan count](https://github.com/osprey-oss/deptry/blob/0.25.1/python/deptry/imports/extract.py).
- [ ] Python import structure = Import Linter for Python packages; check recursive sibling module/package cycles across the applicable scope; enforce layer/forbidden-import/independence contracts where the project defines those boundaries; do not invent a new architecture; missing or incomplete expected analysis fails; existing scope/version/exception policies apply.
- [x] Import Linter result validation = added native text-report capture to the Python template and validation in the existing reader. Import Linter 2.15 fixtures accepted safe imports and rejected sibling cycles plus declared forbidden/layer/independence violations. A cycle twelve levels below the package root passed the native default depth but failed with root + recursive-wildcard ancestors and depth zero; documented that native configuration without adding a graph implementation. Zero contracts, empty/malformed/stale or contradictory reports, warnings, missing packages/configuration/executable, malformed source, nonzero exits followed by a pass, and direct `--contract` filters fail. Installer, deptry, Trivy and parallel/Semgrep regressions passed. The reader verifies reported execution, not complete configured package/contract scope, native exceptions or standalone-script applicability; those remain unchecked. Native references: [recursive siblings](https://import-linter.readthedocs.io/en/stable/contract_types/acyclic_siblings/), [configuration](https://import-linter.readthedocs.io/en/stable/get_started/configure/), [2.15 report rendering](https://github.com/seddonym/import-linter/blob/v2.15/src/importlinter/application/rendering.py).
- [ ] Secret scanning = Gitleaks for repository files + Git history across all languages; fully redact secrets in output; fail on findings/errors; existing scope/version/exception policies apply. User-approved exception: this Hard Eng repository skips Git-history scanning with `scan_git_history: false`; current-file scanning stays required and project templates keep history scanning enabled.
- [ ] Source security = Semgrep for JS/TS + Python + Dart/Flutter; applicable security rules; explicitly fail on findings and scan errors; existing scope/version/exception policies apply.
- [ ] Semgrep validation = prove applicable rules detect known unsafe examples and accept safe examples in each language; validate runtime on the first repository; Dart support is experimental and zero applicable rules is not a pass.
- [ ] Fresh implementation = no legacy adapters, migration machinery, historical exception lists or compatibility scaffolding. Keep only tests proving required behavior and meaningful failure cases.
- [x] Product/design document gate = require root PRODUCT.md and DESIGN.md; reject missing files, empty/whitespace-only content and unfilled `[TODO: ...]` template prompts before running commands. Preserve existing document content.
- [x] Product/design structure = require a Markdown title and filled core level-two sections in template order, without duplicate core sections. PRODUCT.md uses Users, Problem, Product Purpose and Boundaries; DESIGN.md uses Overview, Components and Do's and Don'ts. Optional sections remain allowed; nonvisual projects may omit visual sections. This source repository's documents follow their respective templates and current commands.
- [ ] Product/design context = if documents are missing, study the repository and fill the bundled templates from evidence using product.md and Google's design.md convention, with Atomic Design for UI components. Verify this agent workflow separately; the document gate cannot certify factual accuracy or useful content.
- [ ] Installation = from the target project's root, run `curl -fsSL https://raw.githubusercontent.com/sgaabdu4/hard-eng/main/setup.sh | sh`; copy shared rules, skills and required gate code directly into that project. Canonical skills live in `.agents/skills`; Codex and Copilot discover them directly, only Claude needs links in `.claude/skills`. Preserve existing project files; no submodule, nested Git repository or global installation. The agent adapts missing gate configuration from repository evidence.
- [ ] Plugin scope = affect only the repository where setup runs. Claude uses its project-scoped Context Mode plugin; Codex uses project-local MCP and hook configuration because its plugin installer enables plugins user-wide. Codebase Memory uses each client's native MCP configuration. Native plugin caches are managed by the client; no user-wide plugin activation is added.
- [ ] Agent independence = Hard Eng is agent-agnostic; shared repository configuration, gate commands and results must not depend on a particular AI agent, provider or model; gates also run directly through the CLI and CI.
- [ ] YAGNI = ultra by default throughout Hard Eng; establish the requested behavior, reuse existing code, then prefer stdlib/native platform/existing dependencies before new code; no speculative features, abstractions, configuration or scaffolding; preserve explicit requirements, root-cause correctness and necessary verification.
- [ ] Languages = Dart/Flutter + Python + TypeScript/JavaScript/React only.
- [x] Nonempty test execution = test gates require a fresh supported report with at least one non-skipped completed test. Reject empty, entirely skipped, missing, malformed or stale reports; Flutter output must show a completed successful run and exclude hidden suite-loading events. Use the existing report declarations and runner.
- [x] JavaScript/TypeScript focused declarations = run Biome's native noFocusedTests rule explicitly, with warnings failing the gate; keep the existing full format/lint command. Verify normal tests and focused declarations, including when the project disables the rule.
- [ ] Focused test selection = reject accidental command-line/configuration filters that reduce the expected test suite; verify the applicable Python, JavaScript and Dart test commands. Scanner inclusion/exclusion settings must cover the project's tests.
- [x] Common command-line test selection = reject recognized Python, JavaScript and Dart selector flags, pytest node selectors/PYTEST_ADDOPTS and nested npm script selectors. Arbitrary wrappers, configuration-file filters and positional subsets remain unverified.
- [ ] Coverage = at least 70% executable-line coverage across each affected package's full production code; include files never executed by tests; use the existing test run; preserve stricter existing thresholds.
- [x] Coverage threshold = require a fresh declared coverage report from the same test command and at least 70% covered executable lines. Read Python line counts separately from branch percentages and LCOV line hits for JavaScript/Dart. Reject missing/empty/malformed/stale reports and retain failures from stricter native test commands. Full production-file scope is still covered by the unchecked requirements above and below.
- [x] Declared-source coverage = require each test package's existing language and sources settings; every supported source file in those paths must appear in its fresh coverage report. Normalize report filenames and calculate the threshold only from those files, including unexecuted files. TypeScript declaration files have no runtime code and are omitted. Missing/empty source paths fail. Correct project-specific source boundaries and exclusions still require review.
- [ ] Coverage integrity = exclude test/generated/vendor code, not handwritten production logic to inflate the result; missing, stale or incomplete coverage fails; report branch coverage separately where supported.
- [ ] Conditional MCPs = Sentry when the project uses Sentry; Appwrite when it uses Appwrite; detect actual integration from repository evidence.
- [ ] MCP failure = warn and continue with other available tools when session-start readiness fails; do not deny edits or commands. Report unavailable integrations honestly and do not claim readiness passed.
- [ ] MCP storage = commit setup/configuration; keep credentials, generated indexes, caches and session data untracked.
- [ ] JS/TS formatting + linting = enforce Biome.
- [ ] Updates = check at every LLM session start; automatically install the newest main commit whose required checks passed.
- [ ] Update failure = report a failed update and continue normal work using the existing installed scaffold and its gates. Gate requirements remain active; MCP readiness failures follow the warning-only rule above.
- [ ] Update commits = after a successful update, automatically create a separate commit containing only the Hard Eng update; preserve unrelated work and ask before resolving conflicting local edits. Apply the scaffold-only check scope where eligible; do not automatically push.
- [ ] Scanner exceptions = allow narrowly scoped exceptions for proven false positives/intentional supported patterns; record reason + evidence; keep the rule active elsewhere.
- [ ] File size = maximum 700 physical lines per handwritten source/test file; exclude generated/vendor files; longer files require a file-specific reason + evidence explaining why splitting would harm maintainability; no blanket exemption for existing oversized files.
- [ ] Hook architecture = keep Hard Eng hook scripts in `.hooks` as the single source of truth. Client registrations call those scripts; one shared gate runner owns check execution and results without duplicated gate logic. Add scripts when their updater or gate command exists; do not restore the old coupled implementation wholesale or register missing commands.
- [ ] Hook registration = use `.claude/settings.json` for Claude, `.codex/hooks.json` for Codex and `.github/hooks/hard-eng.json` for Copilot. Map native events to the same shared scripts and preserve existing registrations without adding duplicates. Git pre-push calls the shared implementation through the target repository's Git hook location.
- [ ] AI hook lifecycle = Hard Eng hooks run at session start and completion. Session start checks updates + MCP readiness and warns without blocking work on failure. Add no extra Hard Eng PreToolUse or PostToolUse hooks; leave tool-use integration to Context Mode and wait until completion for automatic code checks. The agent can still run relevant checks while working.
- [ ] AI completion = verify required gates when completing code-changing work. Failed gates require another repair attempt; a crashed, timed-out or otherwise broken verification hook requires repair before claiming completion. Questions, honest blocked reports and user interruptions remain possible; prevent repeated stop-hook loops and never label incomplete verification as passed.
- [ ] Tool selection = prioritize fast execution while preserving required checks.
- [ ] JS/TS gates = Biome + type checks + tests + applicable build + Fallow; React adds React Doctor.
- [x] React Doctor result validation = require fresh schema-3 full React reports, no diagnostics/errors/skipped projects, complete per-project analysis without skipped checks, and matching nonempty analysed-file inventories/counts. Native failure remains failure and the `react` role cannot omit report metadata. Verified locally with 0.9.13: clean React application, accessibility finding, advisory-exit findings, disabled lint, empty source, malformed/stale/missing reports, invalid totals, incomplete sibling project and native command failures; Dart/Gitleaks/installation regressions and Ruff passed. This is report validation, not automatic React gate installation or proof of every rule/source being included. Native contract: [JSON report](https://github.com/millionco/react-doctor/blob/main/docs/json-report.md).

React Doctor probe limitation (2026-09-09): an uncached malformed JSX fixture returned a complete clean report; TypeScript rejected it, so the separate native type gate remains necessary.

- [x] React Doctor scaffold finding = reviewed `command-execution-input-risk` against the session hook. Its pattern treats a Python f-string inside `subprocess.run` as risky, but both package names were fixed literals and no shell or hook payload supplied the arguments. Store the two complete package specifications as literals, removing unnecessary interpolation without changing either command. A fresh installed React repository reproduced the original finding; the uncached full-root scan and installed report reader then passed. A separate unsafe caller-shaped shell-command fixture still failed the same rule. Before/after hook probes confirmed identical argument arrays and no shell; no rule suppression, exclusion or new file/dependency was added. This is a scanner false-positive resolution, not proof that session-update/MCP readiness is implemented. Rule source: [command-execution-input-risk](https://github.com/millionco/react-doctor/blob/main/packages/oxlint-plugin-react-doctor/src/plugin/rules/security-scan/command-execution-input-risk.ts).
- [ ] Dart/Flutter gates = Dart format + analyzer/configured lint plugins + tests + applicable build + Dart Decimate.
- [ ] Duplication = Fallow for JS/TS, Dart Decimate for Dart, jscpd for Python; fail on reported duplicate-code findings/errors; no existing-debt baseline; exclude generated/vendor code and apply the existing narrow exception policy; do not silently skip large handwritten files.
- [x] Python jscpd result validation = require fresh JSON, empty duplicate findings, zero duplication counts and consistent nonempty Python analysis totals; preserve native failure. Verified locally with jscpd 5.2.0: clean/copy/removal, zero-exit findings, malformed/missing/stale reports, invalid counts, inconsistent totals, missing executable and zero analysed sources. The native report lacks a complete file inventory, so this does not prove full source coverage or resolve the broader duplication item.
- [x] Dart Decimate result validation = capture JSON stdout separately from diagnostics; require a fresh combined `check` report with the expected schema/tool, pass verdict, no findings/clones, zero dead-code/duplication counters and threshold, and nonempty analysed source. Native failure remains failure. Verified locally against the installed Cargo build labelled 0.0.23: clean code, cross-file duplicates, unreachable files, syntax errors, empty source, zero-exit findings, malformed/stale reports and invalid counts; a real Dart package cannot omit the report declaration. Dart test-event capture, jscpd/Gitleaks readers and installation regressions passed. This validates reported results, not complete source inclusion or all enabled native rules; the broader duplication requirement remains open.
- [ ] Shared gates = secret scanning + dependency vulnerability checks + applicable integration tests.
- [ ] Dependency vulnerability checks = OSV-Scanner for supported Dart/Flutter, JS/TS and Python dependency files; fail on findings/errors; no packages scanned is not a pass when dependencies are expected; existing scope/version/exception policies apply.
- [ ] GitHub Actions checks = Actionlint only in repositories using GitHub Actions; validate workflows as a shared gate; fail on findings/errors; existing scope/version/exception and result-reuse policies apply.
- [x] Conditional Actionlint setup and built-in checks = setup adds one shared `actionlint -no-color` check for root workflow YAML files, preserving existing shared workflow checks/direct Actionlint commands and unrelated package checks. Empty/non-workflow-only directories add nothing; repeat setup is unchanged. Native Actionlint 1.7.12 accepted valid workflows and rejected invalid expressions, job dependencies, runner labels, empty/malformed YAML, an invalid second workflow, absent workflows and missing executables. A later passing command could not hide failure. Actual local pre-push accepted valid committed workflows and rejected committed invalid YAML despite an uncommitted repair, preserving the receiver/local edit and cleaning up worktrees. Installer preservation and strict typing regressions passed. The official release binary was verified against its published digest and used temporarily; no global installation, wrapper or result parser was added. Optional ShellCheck/Pyflakes integrations were unavailable and are not certified; tool provisioning/version updates, preserved custom-check scope/ignores and remote CI enforcement remain pending. Existing checks are not automatically removed if workflows later disappear. Native reference: [Actionlint 1.7.12 usage](https://github.com/rhysd/actionlint/blob/v1.7.12/docs/usage.md).
- [ ] CI security = Zizmor for GitHub Actions; check permissions, unsafe triggers and related security risks; use a mode that fails on findings/errors; narrowly account for the agreed automatic tool updates under the existing exception policy.
- [x] Conditional Zizmor setup and offline checks = workflow detection now independently adds shared `ci-security` using `zizmor --strict-collection --persona=auditor --format=plain .`, preserving existing Actionlint/Zizmor or role-declared checks without duplication. Native Zizmor 1.30.1 accepted a clean fixture without ignores and rejected excessive permissions, template injection, a dangerous pull-request workflow, informational findings, empty/malformed inputs and missing executables; later passing commands did not hide failures. Default collection returned success while skipping a malformed second workflow; the native strict-collection flag rejected it, so no custom parser was added. Installer/Actionlint regressions passed. Tests used an isolated uv tool environment with `--offline`; the installed command does not force offline mode. Online audits, tool provisioning/version updates, actual project scope/ignores, and any narrow exception for agreed tool updates remain unverified. No global installation, dependency or exception was added. Native reference: [Zizmor usage and exit codes](https://docs.zizmor.sh/usage/).
- [ ] Shell checks = ShellCheck when supported Bash/sh helper scripts exist; fail on findings/errors; existing scope/version/exception and result-reuse policies apply.
- [x] Conditional ShellCheck setup and native failures = setup adds direct shared `shellcheck -- FILE...` arguments for Git-visible `.sh`/`.bash` and extensionless Bash/sh helpers, preserving existing checks without duplication. Discovery fixtures covered no helpers, spaces, ignored files, symlinks and other interpreters. Official ShellCheck 0.11.0, temporarily downloaded and digest-verified, accepted valid scripts and failed through the runner on quoting findings, parse errors, unknown dialects, missing inputs and missing executables. No wrapper or report parser was added. Explicit file lists require maintenance as helpers change; tool provisioning/version updates, sourced-file scope, native exclusions and actual CI integration remain pending. Native dialect reference: [ShellCheck SC2148](https://www.shellcheck.net/wiki/SC2148).
- [ ] Deployment configuration = Trivy configuration scanning when supported Dockerfiles/infrastructure configuration exists; explicitly fail on findings/errors; existing scope/version/exception and result-reuse policies apply.
- [x] Conditional deployment setup = recognizable Git-visible Docker/Containerfile, Terraform and Helm chart filenames add one serial shared `trivy config --exit-code 1 --format json .` check using the existing Trivy report reader. Existing deployment checks/direct Trivy config commands are preserved. Eight filename triggers, ordinary YAML/JSON non-triggers, ignored/symlink inputs, repeat setup and existing-check preservation passed fixtures. Full installation added the expected command and remained byte-stable on repetition; the installed runner accepted a clean Dockerfile and rejected unsafe/malformed Dockerfiles with native Trivy 0.74.0. Kubernetes/CloudFormation/ARM and custom filenames need explicit applicability review; tool provisioning, native inclusion/ignores and remote CI remain pending. Supported-format reference: [Trivy infrastructure coverage](https://trivy.dev/docs/dev/docs/coverage/iac/).
- [x] Trivy result validation = require fresh schema-2 filesystem/config JSON with nonempty checked targets, positive successful-check counts, zero failures and no reported incomplete/suppressed findings. Preserve native failure and capture JSON separately from stderr. Native Trivy 0.74.0 verified clean/unsafe/repaired Dockerfiles, successful-check details, zero-exit findings, empty/invalid configuration, malformed/stale reports and invalid counts. A mixed clean/malformed input exited zero and omitted the malformed target from JSON; the runner now rejects logged errors and missing scan-start diagnostics, including quiet-mode concealment. Regressions for Dart/React/Gitleaks reports and installation passed. This validates reported results and errors in the tested native logging format; automatic applicability, all infrastructure formats, source inclusion and severity/ignore configuration remain separate. Native [report structure](https://github.com/aquasecurity/trivy/blob/v0.74.0/pkg/types/report.go) and [misconfiguration statuses](https://github.com/aquasecurity/trivy/blob/v0.74.0/pkg/types/misconfiguration.go).
- [ ] Lockfiles = validate declaration/lockfile consistency with the existing package manager during dependency changes + CI; no reinstall on every edit.
- [ ] Generated source = when committed, regenerate affected outputs with the existing generator and check consistency.
- [ ] Changed UI = small runtime journey test + applicable automated accessibility checks; reuse browser/device tests; no full-app crawl by default.
- [ ] Mutation testing = supported but optional; present scope + estimated runtime and obtain user acceptance before running.
- [ ] Mutation scope = complete changed production functions + relevant covering tests, including existing tests; explain when broader scope would help.

## Integration and enforcement verification

- [ ] MCP integrations = include context-mode + codebase-memory-mcp in repository-local setup; expose their tools to the active agent.
- [ ] MCP readiness = setup + session-start checks; prove real MCP calls, required Context Mode routing, and correct repository/index state for Codebase Memory.
- [ ] Service MCP readiness = verify intended Sentry organization/project or Appwrite endpoint/project with a read-only call; apply only to integrations the project uses.
- [ ] Full checks = task completion + pre-push + CI; run required checks directly each time under the check-scope rules, including the scaffold-only exemption.
- [ ] Hook capability = verify supported events and blocking semantics for each agent/version; test findings, crashes, timeouts and invalid responses; explicitly report unsupported enforcement and never treat a notification-only or failed hook as a working blocker.
- [ ] Container dependencies = extend OSV-Scanner to produced container images when the project builds them; scan image packages in addition to source dependency files; existing scope/version/exception and result-reuse policies apply.
- [x] Container image result validation = add report type `osv-image` for `container-vulnerabilities`, requiring OSV package/finding checks plus nonempty layer metadata, valid nonempty-layer digests and package origins pointing to those layers. Source-only reports cannot satisfy this role. Reuse the reader mapping in the existing report module instead of duplicating scanner names in the runner. Native archive fixtures passed clean npm packages and empty history entries, rejected vulnerable npm/Alpine packages and empty/malformed/missing archives, and proved invalid/stale reports, missing origins and zero-exit findings fail. Verified with installed OSV 2.4.0 and latest 2.5.1 in a temporary location; no global installation or source dependency was added. All scanner-reader regressions, installer preservation, local pre-push reader checks and Ruff passed.

Container completeness remains open: both OSV 2.4.0 and 2.5.1 returned success after silently omitting malformed `node_modules/.package-lock.json` from an archive containing a second valid package. The JSON included no error; layer/package validation cannot detect that omission. The image metadata also does not identify the intended built image. Docker's daemon was unavailable, so these are Docker-format archive fixtures, not a verified Docker build/save or direct-image workflow. Automatic image applicability, extraction completeness and binding checks to every produced image remain unchecked. Native contracts: [image metadata](https://github.com/google/osv-scanner/blob/v2.4.0/pkg/models/image.go), [npm artifact extraction](https://github.com/google/osv-scanner/blob/v2.4.0/internal/scalibrextract/language/javascript/nodemodules/extractor.go), [archive scanning](https://google.github.io/osv-scanner/usage/scan-image).

Extraction-error investigation on 2026-09-09: OSV 2.5.1's container path checks the top-level scan error and empty inventory, then retains inventory/image metadata without examining plugin statuses. Its filesystem path separately processes extraction statuses. The image CLI exposes no strict-extraction option; switching the same malformed archive to SARIF still returned zero with empty `invocations` and no extraction error on stderr. No supported flag or report-format remedy was established. The smallest proposed correction is in OSV's container scan owner: propagate failed extraction statuses and return failure, then verify the mixed valid/malformed archive against that release. This is an upstream proposal, not an implemented Hard Eng fix; no scanner fork, custom package parser or replacement scanner was added. Source: [container result handling](https://github.com/google/osv-scanner/blob/v2.5.1/pkg/osvscanner/osvscanner.go#L246-L282), [filesystem extraction errors](https://github.com/google/osv-scanner/blob/v2.5.1/pkg/osvscanner/scan.go#L340-L372).

## Efficiency review — 2026-09-09

The user requested a review of performance, parallelism, batching, queues, chunks and Big-O across Python, JS/TS and Dart/Flutter. The runner changes and selected native performance/async rules above are now implemented. The table records the initial gap review; the implementation status and follow-up checklist below supersede its initial configuration observations. Remaining application-level checks are proposals for the next agreed steps, not enabled gates or permission to add infrastructure. Typing, coverage and complexity checks do not establish application efficiency.

| Area | Current coverage and evidence | Proposed next action and limit |
| --- | --- | --- |
| Python | The template explicitly adds C901/branch/statement checks, not an explicit performance policy. [C901 measures control-flow complexity](https://docs.astral.sh/ruff/rules/complex-structure/), not asymptotic running time. Existing Ruff can detect [quadratic list summation with RUF017](https://docs.astral.sh/ruff/rules/quadratic-list-summation/) and [blocking HTTP inside async functions with ASYNC210](https://docs.astral.sh/ruff/rules/blocking-http-call-in-async-function/); actual enabled rules depend on the tool version and project configuration. | Verify and explicitly retain useful stable rules in the existing lint gate, starting with RUF017 and applicable blocking-I/O rules. Avoid adding another linter. Do not enable every PERF rule just for its name: Ruff describes [PERF401 as a micro-optimization](https://docs.astral.sh/ruff/rules/manual-list-comprehension/). These patterns do not prove end-to-end throughput or Big-O. |
| JavaScript/TypeScript | Existing Biome provides [noAccumulatingSpread](https://biomejs.dev/linter/rules/no-accumulating-spread/javascript/) for repeated accumulator copying, but the scaffold does not explicitly preserve that rule against project overrides. [noFloatingPromises](https://biomejs.dev/linter/rules/no-floating-promises/javascript/) is currently nursery, type-domain and informational by default; `--error-on-warnings` alone is not proof that it blocks. | Verify the stable accumulator rule with safe/unsafe examples and retain it through the existing lint configuration. Evaluate promise handling against the actual project before adopting experimental rules or a second type-aware linter. Keep Node event-loop, worker-pool and browser responsiveness checks tied to the affected flow. |
| Dart/Flutter | The analyzer runs, but the installed strict-type rules do not explicitly include performance/async rules. Native [avoid_slow_async_io](https://dart.dev/tools/linter-rules/avoid_slow_async_io), [unawaited_futures](https://dart.dev/tools/linter-rules/unawaited_futures) and [discarded_futures](https://dart.dev/tools/linter-rules/discarded_futures) cover specific patterns. | Validate useful native rules on the actual supported code and preserve intentional handled background work. Review expensive work on the main isolate, lazy list construction and repeated widget work. Use [Flutter profile-mode integration measurements](https://docs.flutter.dev/cookbook/testing/integration/profiling) on a representative device for affected UI performance; ordinary analyzer/unit-test passes are insufficient. |
| Parallelism and queues | Async is not automatically CPU parallelism. [Python documents blocking work in asyncio](https://docs.python.org/3/library/asyncio-dev.html); [Dart documents isolate execution and transfer costs](https://dart.dev/language/concurrency); [Node documents event-loop and worker-pool blocking](https://nodejs.org/en/learn/asynchronous-work/dont-block-the-event-loop). | Where a flow fans out, test a bounded active-worker count AND bounded pending work, completion/error propagation, cancellation/timeouts and saturation behavior. Use existing native pools/semaphores/queues where needed; a semaphore around millions of eagerly created tasks does not bound pending-task memory. Do not mandate queues or workers for simple sequential code. |
| Batching and chunks | Native APIs and stream backpressure can prevent repeated I/O and unbounded buffers: [Node stream backpressure](https://nodejs.org/en/learn/modules/backpressuring-in-streams), [Python bounded queues](https://docs.python.org/3/library/asyncio-queue.html). Batch limits and ordering depend on the actual service contract. | For affected bulk flows, test empty input, exact batch boundaries and a final partial chunk; assert no missing/duplicate records, required ordering, bounded memory, API size/rate limits and partial-failure/retry behavior. Assert meaningful request/query counts to catch N+1 I/O. Choose chunk sizes from payload and service limits, not a global magic number. Reuse streams/pagination/native batch calls before adding a queue service or library. |
| Big-O and measured budgets | Cyclomatic/cognitive complexity, file length and coverage do not establish time or space complexity. A nested loop is not inherently wrong, and parallel execution does not change an algorithm's total asymptotic work. | For changed hot paths, name the input dimensions, expected time/space growth, repeated scans/copies/sorts and query counts. Prove relevant operation counts or scaling at representative input sizes, including skewed/worst cases. Use repeatable runtime/memory/latency budgets only for a real requirement on a controlled environment; timing samples cannot prove a general Big-O bound. Reuse existing tests/profilers, not a universal Big-O scanner. |
| Gate execution cost | The scheduler now uses a linear setup/ordinary partition, at most two futures and bounded-memory copying of temporary command logs. It does not partition scanner source files. [Python documents that thread-pool chunksize has no effect](https://docs.python.org/3/library/concurrent.futures.html). [Semgrep warns against oversubscribing native workers](https://semgrep.dev/docs/cli-reference). | Measure actual project wall time and memory before increasing concurrency. Keep native whole-program scans intact; arbitrary file chunks can miss cross-file imports, dead code or duplicates. Reuse native caching/batching where it preserves complete current findings; no custom result-cache framework. The two-check bound does not limit each tool's own threads, memory or output disk use. |

Suggested implementation order: verified stable native performance/async rules, then project-specific batching/concurrency regression checks where those flows exist, then measured performance budgets for identified hot paths. Do not impose benchmark infrastructure, a queue service, arbitrary latency limits or a global ban on nested loops.

Do not mechanically replace sequential `await` with `Promise.all`, `asyncio.gather` or `Future.wait`: dependency order may be required, and eagerly starting the whole input can exhaust connections or memory. A bounded queue also needs defined behavior when full; retries must not amplify overload. Dart's [discarded-future rule has a history of handling-pattern disagreements](https://github.com/dart-lang/sdk/issues/59887), so test intentional future ownership before treating it as a blanket requirement.

Implementation status and proposed follow-ups (unchecked work requires the next step's approval):

- [x] Enable the selected stable subset: Python RUF017/ASYNC210, JS/TS noAccumulatingSpread and Dart unawaited_futures; verify positive/negative examples, global rule-off settings, relevant Dart nested settings and preservation of existing typing checks. These extend existing native commands rather than add scans.
- [x] Scaffold efficiency guidance = route to an efficiency reference requiring concrete pass/fail contracts for relevant bulk, async and hot-path changes. Use controlled inputs/synchronization and existing gated tests to assert batch outputs/counts, active/pending limits and failure/cancellation behavior; record unknown limits as unverified. General Big-O and runtime budgets still need project-specific reasoning/evidence. Do not mandate queues, batching, benchmarks or tracking files for unrelated flows. This records authored and installable guidance, not an automatic universal performance gate or proof of agent compliance.
- [ ] For actual bulk/async flows, add only the missing batching, queue-bound, cancellation/error and request-count regression checks to existing suites.
- [ ] For identified hot paths, review time/space complexity and add a representative scaling or operation-count check; introduce measured latency/memory budgets only from a real requirement.
- [ ] Measure real gate wall time and resource use before tuning native worker counts or adding more parallel checks; preserve full analysis scope.

Initial research probes with Ruff 0.16.4 and Biome 2.5.12 established these native capabilities; they have since been integrated and verified as recorded below. Dart's stable `avoid_slow_async_io` is deliberately not a global default: its [documented remedy uses synchronous file I/O](https://dart.dev/tools/linter-rules/avoid_slow_async_io), whose effect on responsiveness needs project evidence. Dart discarded_futures and Biome's experimental noFloatingPromises remain deferred. The enabled subset does not certify every API pattern, source exclusion, exception or real workload performance.

## Acceptance proof

Gitleaks result validation verified locally on 2026-09-09 with Gitleaks 8.30.1. Real clean file/history scans passed concurrently; a synthetic secret failed, removal restored the current-file pass, and the history scan still found the deleted secret. The synthetic value was absent from captured output and both SARIF reports. Findings failed with native `--exit-code 0` and when replayed behind a successful command. Missing/stale/malformed output, absent rules/results, wrong scanner/version, missing executable, invalid input and nonzero exit with clean output failed; a passing sibling could not mask failure. Native [SARIF](https://github.com/gitleaks/gitleaks/blob/v8.30.1/report/sarif.go) was chosen over the findings-only [JSON format](https://github.com/gitleaks/gitleaks/blob/v8.30.1/report/json.go) because it also carries the scanner and configured rules. It still has no complete scan inventory, and its reported tool version is a static value in the tested release; neither full coverage nor the actual installed version is inferred from that field.

The Hard Eng skill now has a 17-line router and four focused references, with all former instruction entries accounted for. Relative template links were rebased, the absent CI-template link was replaced with an explicit integration requirement, and command instructions now name the available runner rather than imply missing entrypoints work. The efficiency reference specifies observable assertions and how to include them in existing gate commands; it does not add a generic Big-O checker. Native skill validation and every local Markdown link passed. Fresh installation copied the router/references exactly, repeat installation was unchanged, source DECISION.md remained undistributed, and a local reference conflict rejected before writes. Gitleaks cases, existing Fallow/OSV/Semgrep/parallel regressions, Ruff lint/annotations/format and diff checks passed. Four reference files serve the user's explicit router/reference structure; no new runtime file or dependency was added. Agent compliance and project-specific efficiency tests remain separate verification work.

Efficiency guidance verified locally on 2026-09-09: a fresh temporary project received the exact updated `.agents/skills/he/SKILL.md`; repeat installation left its files unchanged, and source-only `DECISION.md` was not distributed. The change adds only three conditional guidance entries covering bulk boundaries/failures, bounded active/pending async work and time/space scaling. Diff checks passed. Agent compliance and application-specific performance outcomes remain unverified; no new runtime gate, dependency or benchmark was added.

Selected performance/async rules verified locally on 2026-09-09 with Ruff 0.16.4, Biome 2.5.12 and Dart 3.13.2. The actual runner/native commands rejected Python quadratic list summation and blocking HTTP in async functions, accepted linear flattening and offloaded HTTP, and retained annotation failures. JS and TypeScript spreading accumulators failed while append accumulation passed; var/any rejection remained active. Ruff global ignore/empty-selection settings and Biome rule-off settings did not suppress the explicit checks; disabling the Biome linter failed. Dart rejected an accidentally unawaited future and accepted awaited/explicitly unawaited work; root/nested rule disabling and ignored diagnostics failed, with repair restoring a pass. Installation preserved additional project checks and repeated unchanged in all three stacks. Existing strict typing and real pytest/Flutter report regressions passed, as did Ruff lint/format and diff checks. Only two template command lists and the existing Dart required-rule map changed in executable configuration; no additional check invocation, dependency or source file was introduced. These checks cover the configured native scan scope and named patterns; they do not prove general Big-O, runtime latency, complete source inclusion or exception correctness.

Parallel execution and Semgrep report validation verified locally on 2026-09-09. Real subprocess timestamps proved two-check overlap, a maximum of two running checks, immediate reuse of a completed slot, setup-before-checks and serial barriers. Missing/nonzero/timed-out setup stopped later work; concurrent command failures/timeouts remained failures alongside passing siblings. Declared report collisions and invalid flags failed before execution, independent concurrent reports stayed separate, and child output stayed grouped. Scheduling uses O(n) partitioning plus O(n) dispatch for n checks with a fixed worker bound; the manifest and ordered check lists still require O(n) memory. Temporary logs avoid buffering all child output in RAM. No general speedup or CPU/memory ceiling is claimed.

Semgrep 1.176.1 native local rules accepted safe and rejected unsafe Python, JavaScript, TypeScript and Dart examples; repairs restored passes. Real findings replayed behind an exit-zero command still failed. Missing/stale/malformed results, zero rules/files/source content, reported scan errors/skipped work/timeouts and inconsistent scope failed. Real parser errors with applicable patterns, empty files and irrelevant-language input also failed. Rule prefiltering can avoid parsing irrelevant files; an invalid file without an applicable pattern produced no error, so the reader cannot certify complete parsing or unreported exclusions. Production registry rule coverage is not certified, and the Dart template's `.agents/hard-eng/security` rule directory is still absent. Existing native Fallow/OSV, strict typing, pytest/Flutter coverage, installer preservation and local pre-push reader tests passed. Existing test/coverage readers moved unchanged apart from type annotations into `.hooks/reports.py`; no new source file or runtime dependency was added for this step. Native references: [Semgrep CLI](https://semgrep.dev/docs/cli-reference) and [JSON fields](https://semgrep.dev/docs/semgrep-appsec-platform/json-and-sarif).

OSV reports verified locally on 2026-09-09 with OSV-Scanner 2.4.0: real npm lockfile scans passed `is-number` 7.0.0 and rejected vulnerable `lodash` 4.17.15, empty input and invalid input. The real vulnerable JSON also failed when replayed by a command exiting zero. Fixtures rejected missing/stale/malformed results, incomplete package identities, reported errors, vulnerability groups and generic findings. A clean report could not hide a nonzero command exit or be reused by a later command. Installer repeat/conflict tests passed for `.hooks/reports.py`, which holds the unchanged Fallow reader and the new OSV reader so the runner stays below 700 lines; no new dependency was added. Actual pushes to a temporary local receiver passed with the committed reader and failed when it was missing from the commit despite an uncommitted repair, preserving the receiver and cleaning up worktrees. Fallow, real pytest/Flutter report handling, installer, Ruff lint/annotations/format and diff checks passed. This verifies report validation, not every ecosystem's dependency discovery, scanner configuration exceptions or completeness against every declared dependency. Native contract: [OSV results](https://github.com/google/osv-scanner/blob/v2.4.0/pkg/models/results.go).

Fallow reports verified locally on 2026-09-09 with Fallow 3.22.0: the configured native command returned exit zero despite a real unused-file finding, which the runner now rejects from JSON. Real clean code passed, reachable duplicated code failed, removing findings restored a pass, and a source-empty project failed. Report fixtures rejected malformed/empty/missing sections, invalid counters, contradictory findings, analysis diagnostics, skipped comparisons/hidden clone groups, stale output and zero entry points. Nonzero/missing commands and outside-package report paths failed; a later passing command did not erase failure. Real pytest JUnit/coverage and Flutter machine-output/coverage regressions passed, and skipped-only Flutter execution still failed. Ruff lint/format and diff checks passed. Fallow's duplication file count includes only files meeting its token threshold, so zero eligible duplication files alone is not rejected for a valid small project. The report does not enumerate every scanned file or expose every parser omission; full project scope and tool-internal completeness are not certified by this check. Native contracts: [combined report](https://github.com/fallow-rs/fallow/blob/v3.22.0/crates/api/src/combined_output.rs) and [duplication statistics](https://github.com/fallow-rs/fallow/blob/v3.22.0/crates/types/src/duplicates.rs).

Strict native typing verified locally on 2026-09-09 with TypeScript 7.0.2, Dart 3.13.2, Pyrefly 1.0, Ruff and PyYAML 6.0.3. Real compiler/analyzer checks accepted typed functions with inferred locals and JavaScript JSDoc, and rejected missing parameter types, nullability/property/catch errors, explicit Dart dynamic, raw generics and unsafe downcasts. Actual runner checks rejected missing typing roles, relaxed Pyrefly settings, omitted declared TypeScript files, nested Dart relaxation, and local/package-included Dart exclusions or ignored diagnostics. Python's root configuration and explicit source filenames prevented nested configuration and exclusion bypasses. Existing Dart lint rules and project typecheck commands remained active; repeat installation, installer preservation, command failure/timeout, dependency ordering and Ruff lint/format/diff checks passed. Missing PyYAML failed clearly. These are strict native static checks over the declared project scope; they do not prove that every value returned by an untyped external API has a precise type, or replace runtime input validation. Full scanner, agent-hook and CI acceptance remains tracked separately. Native references: [TypeScript compiler options](https://www.typescriptlang.org/docs/handbook/compiler-options.html), [Dart analysis configuration](https://dart.dev/tools/analysis), [Pyrefly configuration](https://pyrefly.org/en/docs/configuration/).

Typing defaults and common test selectors verified locally on 2026-09-09: actual Pyrefly 1.0 and Ruff accepted an annotated function with inferred locals and rejected missing annotations, an incorrect return type and imported Any through the relevant checks. Installation added native strict settings, repeated without changes and preserved conflicting settings without writes. Biome 2.5.12 accepted inferred `const` and rejected `var`, explicit `any` and uninitialized declarations. Selector fixtures covered nested npm scripts and PYTEST_ADDOPTS; actual runner invocations rejected selected suites for all three languages. Existing real Python/Node coverage regressions, Ruff lint/format and diff checks passed. TypeScript compiler strictness, Dart dynamic checks, configuration overrides and complete project typing scope are not yet certified.

JavaScript/TypeScript focused declarations verified locally on 2026-09-09 with cached Biome 2.5.12 and the generated gate configuration: normal tests, parameterized tests and comments/strings passed; `test.only`, `describe.only`, `it.only` and `test.only.each` failed with the native noFocusedTests diagnostic. Turning the rule off in project settings did not bypass the explicit check; disabling the entire linter failed the gate. A later passing command did not hide failure. The existing full lint command was preserved, and diff checks passed. This verifies native declarations within Biome's scanned files, not all framework/filter mechanisms. Native rule: [Biome noFocusedTests](https://biomejs.dev/linter/rules/no-focused-tests/).

Declared-source coverage verified locally on 2026-09-09: all three report formats rejected omitted source files and counted zero-hit files; unrelated report entries could not inflate the percentage, and absolute/relative filenames matched correctly. Real pytest-cov included an unimported file at zero coverage, lowering the measured result. Real Node and Flutter reports that omitted an unimported file were rejected despite their otherwise passing coverage. Empty/missing source declarations and TypeScript declaration-file handling were tested. Command/dependency regressions and Ruff lint/format/diff checks passed. This verifies the declared source boundary; it does not certify that a project's declarations or exclusions correctly represent every production file or executable line.

Coverage threshold verified locally on 2026-09-09: actual pytest-cov, Node LCOV and Flutter LCOV runs passed full line coverage and failed below 70%. Report fixtures verified the exact 70% boundary, rejection of 69.99%, no executable lines, missing/stale/malformed data, truncated LCOV and preservation of native command failures. Duplicate LCOV entries combine hits without inflating the denominator; Python uses line counts rather than its combined branch percentage. Empty-test, command and dependency-order regressions plus Ruff lint/format/diff checks passed. Full production-file inclusion, exclusion integrity and branch reporting remain pending. Native format references: [Coverage.py totals](https://coverage.readthedocs.io/en/7.12.0/faq.html) and [LCOV line records](https://github.com/linux-test-project/lcov/blob/master/docs/man/geninfo.rst).

Nonempty test execution verified locally on 2026-09-09: actual pytest and Flutter runs accepted a completed test and rejected skipped-only/empty runs; actual Node JUnit output accepted a test and rejected skipped-only runs. Report fixtures additionally proved empty, skipped-only, failed, malformed and stale JUnit rejection, and hidden/skipped/incomplete Flutter-event rejection. Flutter's real machine output includes process-event arrays, which the reader handles alongside test events. Existing command, dependency-order and document regressions plus Ruff lint/format/diff checks passed. Coverage and accidental focused-test detection remain unchecked. Native format references: [pytest JUnit output](https://docs.pytest.org/en/stable/how-to/output.html) and [Dart test JSON events](https://dart.googlesource.com/test.git/+/9db7e039/pkgs/test/doc/json_reporter.md).

Document structure verified locally on 2026-09-09: both files rejected missing/empty core sections, duplicate core sections, incorrect core section order and missing titles before commands ran. The revised source documents passed in the temporary project, as did a design with optional visual sections. Existing missing/empty/template-prompt checks still passed, with document bytes preserved. Ruff lint/format and diff checks passed. Structural checks do not certify the documents' accuracy, usefulness or project-specific design coverage.

Pre-push launcher repair verified locally on 2026-09-09: actual pushes to a temporary local receiver now fail when the local runner file is missing, with the receiver unchanged. Passing committed code, failing committed code with an uncommitted fix, a missing executable and a broken committed runner were retested; expected allow/block behavior, local-edit preservation and worktree cleanup passed. Installer conflict/repeat checks and replacement of the recognized old shortcut passed, as did Ruff lint/format and diff checks. This resolves the missing-runner failure recorded below; full applicable scanner enforcement remains pending.

Pre-push verification on 2026-09-09 used temporary local Git repositories and actual pushes. Passing committed code reached the receiver; failing committed code with an uncommitted fix, a missing executable, and a broken committed runner were blocked. Local edits were preserved and temporary worktrees cleaned up. A remaining failure keeps the Git pre-push item unchecked: deleting the local `.hooks/hard-eng.py` leaves a dangling pre-push symlink, which Git skips; the local test receiver accepted the push. Repair the hook entry point before claiming fail-closed enforcement.

Document gate verified locally on 2026-09-09 in a temporary project: both required files were tested missing, empty, whitespace-only and containing bundled or lowercase template prompts. Each failure named the file and stopped before the configured command ran. Filled documents allowed execution; repairing a document restored execution; no document content was changed. Existing runner/dependency-order tests and Ruff lint/format/diff checks passed. Only the presence/content gate is verified, not research or document authoring by agents.

Dependency ordering verified locally on 2026-09-09 using real subprocesses in a temporary two-package project with shared checks: all three setup commands ran first, ordinary check order was preserved, and nonzero/missing/timed-out setup commands stopped the remaining commands. Existing runner regression checks, Ruff lint/format and diff checks passed. This verifies ordering and failure handling; it does not establish successful dependency installation or package-manager adaptation in real projects.

Template command compatibility verified locally on 2026-09-09: setup generated a mixed Python/JavaScript/Dart project configuration, and the runner dispatched all 27 commands with exact arguments and working directories using fixture executables; an injected command failure failed the run. An actual Ruff formatting command accepted formatted Python and rejected unformatted Python. All template changes were limited to moving each executable into its command list; checks, flags and report metadata were preserved. This supersedes the earlier separate-`tool` incompatibility below, but does not prove the other scanners, dependency preparation, report validation or latest-tool requirements.

Basic command execution verified locally on 2026-09-09 in a temporary project: real package/shared commands, nonzero exit followed by a passing check, missing executable, ten invalid/empty/unsupported configurations, rejection before side effects, missing/invalid JSON, and a real subprocess timeout using a shortened test timeout (normal default remains 600 seconds). Ruff lint, formatting and diff checks passed. This proves command execution only, not scanner completeness or native agent/pre-push/CI enforcement. Existing templates with separate `tool` fields are explicitly rejected pending their own implementation step.

Installer preservation verified locally on 2026-09-09 using `python3 /Users/abid/hard-eng/setup.py` against temporary Git repositories: initial and repeat installation, replacement of shared instructions while preserving project text exactly, existing settings and gates, nine configuration/instruction/skill/hook conflicts, and symlink/directory/non-root destination rejection without writes. Ruff lint, formatting and diff checks passed. These two checked items cover installer behavior only; automatic updates, native client loading, gate execution and CI remain unverified. The documented `bin/hard-eng` command is currently absent.

- Verify setup and repeat updates in an existing project, preserving its instructions, configuration and unrelated work; prove AGENTS.md contains only one Hard Eng section.
- Prove session-start updates, failure fallback, conflict handling and isolated update commits. Prove scaffold-only updates avoid application checks while project and mixed changes receive applicable checks.
- Verify real MCP calls and instruction/configuration loading in fresh Claude, Codex and Copilot sessions. Verify each client's supported project-local integration and blocking behavior before claiming support; do not substitute global activation.
- Verify completion, pre-push and CI enforcement with passing and failing changes. Verify remote branch protection only after separate approval.
- Verify session-start update/MCP failures leave edits and commands available; completion failures request repair without endless loops, and failed or broken pre-push checks block pushes. Verify all client registrations call the shared `.hooks` scripts without duplicated execution.
- Verify conditional service readiness and deployment/container scans in applicable projects or representative fixtures; do not claim success from absent integrations or empty scans.
