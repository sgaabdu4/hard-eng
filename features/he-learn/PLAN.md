# Repeated-failure learning and terse decision capture

Status: Complete

## Outcome + scope

Implement HE Learn with native hooks throughout the supported agent workflow: prompt the agent to inspect repeated failures and meaningful decisions, prefer deterministic prevention at an existing owner, and use skills only when executable prevention is insufficient. Repository skills live under `.agents/skills/`; terse ADRs live under `docs/adr/`. Preserve normal task progress and existing verification. No source commit/push, global installation, unrelated project changes or external publication.

## Repository context

`.hooks/agent_hooks.py` owns session/completion responses; `setup.py` registers Claude, Codex and Copilot hooks and preserves project-owned configuration. The runner dispatches those events. Research already owns recurrence diagnosis, Writing Great Skills owns skill creation/repair, and the existing plan holds task proof. The reviewed proposal is `/tmp/he-learn-proposal-20260911.md`; the user now explicitly adds ongoing hook prompts, deterministic-first recurrence prevention and ADR capture, overriding the proposal's no-new-hook starting scope.

Source HEAD is `bd9dc5a308304ae5666e3cd59bc558d676e2d76d` with pending shipping and prior plan changes. Preserve them. `feature/he-learn` isolates this work without committing or discarding existing edits.

## Decisions + authorization

Blockers: None

The user explicitly requested implementation, hooks during the entire process, repeated-failure prevention with skills as a last resort, terse ADRs following Writing Great Skills, `docs/adr/` for decisions and `.agents/skills/` for skills. Existing Autonomous participation and bounded subagent research/verification apply. Native client compatibility is a technical fact to establish, not a user decision. Hook prompts make the agent inspect evidence; they cannot certify semantic learning or universal non-recurrence.

## Acceptance + steps

- [x] Add a short HE Learn skill and relevant workflow routes. Metadata, links, installed discovery, repeated-failure selection, deterministic-first prevention and justified skill fallback pass actual agent outcome checks.
- [x] Extend the existing native hook owner and installer across supported start/prompt/tool/completion boundaries. Codex native context and failed-tool delivery, all-client command/schema tests and duplicate suppression pass; Claude/Copilot context-consumption and native resume remain explicit external gaps below.
- [x] Learning hooks preserve tool results, avoid treating arbitrary text as a proven recurring cause, and do not run full gates after every tool. Success, failure, malformed input, interruption and loop behavior are covered by focused regressions and native probes; no semantic classifier or new Stop loop was added.
- [x] Define terse `docs/adr/` records for meaningful accepted decisions/steering, with status, context, decision and consequences/proof. Native capture, later retrieval and supersession preserve the earlier rationale; routine steering creates no ADR and an investigation idea remains unaccepted.
- [x] Prefer repair of existing invariants/tests/checkers/lints/hooks; require original-failure plus nearby-valid proof. The skill-fallback fixture names its absent executable sending boundary, repairs the existing skill, and a fresh consumer correctly handles both pending and confirmed evidence under completion pressure. No duplicate learning database or global memory writes.
- [x] Preserve installer conflicts, prior checks and source-only instructions. Focused regressions and independent actual-diff review verify these behaviors; bounded native proof and external limitations are recorded separately. Final Complete validation is recorded below.

## Baseline + execution

Result: Passed
Evidence: `uv run python .hooks/hard-eng.py check --plan-stage Draft` passed all 17 gates, 370 tests and four performance tests in 101.53 seconds on the starting implementation. Log: `/tmp/he-learn-baseline-20260911.log`.

Readiness: `uv run python .hooks/hard-eng.py check --plan-stage Ready` passed all 17 gates, 370 tests and four performance tests in 72.44 seconds before production edits. Log: `/tmp/he-learn-ready-20260911.log`.

One source coordinator owns implementation and integration. A bounded research worker probes Claude/Copilot native event contracts in disposable fixtures; Codex and source integration stay with the coordinator. Start with the shared context prompt and a native event round trip, then add only necessary event-specific handling and decision guidance. Existing hook/runtime files are sufficient unless focused implementation exposes a concrete owner/size constraint.

Hook design: reuse the existing handler for small, stateless context checkpoints at start, supported user-prompt and tool-result boundaries; add the native failure event where supported. The agent inspects its existing conversation evidence; no repeated-error string classifier or transcript/learning database is added. Ordinary tool output is preserved. Existing Stop verification and its loop guard remain, with a learning hint on failed checks; do not force an extra successful Stop loop. Official Codex documentation confirms `PostToolUse` includes nonzero Bash exits and supplies `hookSpecificOutput.additionalContext`. Claude supports prompt/tool/failure context; Copilot config-file prompt output is dropped, so use its session/tool boundaries. Verify the installed clients rather than assuming documentation proves runtime delivery: [Codex](https://learn.chatgpt.com/docs/hooks), [Claude](https://code.claude.com/docs/en/hooks), [Copilot](https://docs.github.com/en/copilot/reference/hooks-reference).

## Risks + recovery

Native hook schemas and event coverage differ by client; current official documentation and real installed-client probes determine registration. Hook output is guidance rather than semantic proof. Do not classify recurrence from an error string alone or persist raw transcripts/secrets. Keep hook work bounded and nonblocking for ordinary progress; completion still reports genuine gate failures. New prompts must not hide tool failures, create a stop loop, manufacture ADRs or broaden authorization. Failed validation returns to the responsible owner while preserving evidence.

## ux_reference

N/A — this change supplies CLI hook responses and repository guidance. Native context delivery and agent outcomes are the relevant interface proof; no product screen is introduced.

## Verification

Result: Passed
Evidence: After the expanded adversarial audit and its two repairs, final Complete passes all 17 gates, 388 tests, four performance tests and 84.31% executable-line coverage, exit 0 (`/tmp/he-learn-edges-complete-20260911.log`). Focused hook/installer tests pass 100 cases (`/tmp/he-learn-edges-focused-final-20260911.log`). Both subagents have completed review; no confirmed finding remains open. Native metadata validation, changed Markdown links, source-ADR exclusion in installation and diff whitespace checks pass. Native behavior passes its bounded acceptance cases; external client gaps remain explicit. Earlier implementation proof remains at `/tmp/he-learn-final-complete-20260911.log` and `/tmp/he-learn-integrated-20260911.log`. Ready for ship — local implementation and verification complete; this coordinator performed no delivery.

Review repair: malformed saved session arrays/null or invalid Git-base values now produce a structured Stop blocker instead of crashing. Four regression cases fail before validation and pass afterward; existing unchanged-session and loop behavior remain covered. Logs: `/tmp/he-learn-state-before.log` and `/tmp/he-learn-focused-final-20260911.log`. No new runtime file/helper was needed. The first Complete attempt rejected an unchecked review step before running tools (`/tmp/he-learn-complete-20260911.log`); it was not a passing check.

### Native outcomes + limitations

Expanded adversarial verification requested by the user after implementation: two independent subagents own hook/installer boundaries and ADR/authority semantics; the coordinator owns recurrence diagnosis and deterministic-first selection. The earlier Complete results apply to the earlier verified snapshot. Source HEAD independently advanced to `326d43f` during this audit; the tested hook and HE Learn skill bytes match that checkout. This coordinator made no commit.

| Expanded edge coverage | Actual outcome + receipt |
| --- | --- |
| Hook/installer adversarial matrix | Initial 42/42 CLI/handler cases passed: malformed payload/state, invalid bases, session baseline preservation, unchanged/retried Stops, command failure/success, output schemas, secret-payload isolation, Copilot/Claude duplicate suppression, installer repeat/preservation/conflict behavior. `/tmp/he-learn-adversarial-20260911.json`. The timeout case injects `TimeoutExpired` into the handler; it proves structured exception handling, not a real elapsed timeout or native client timeout. Further coordinator probes then found the two cases below that this initial matrix missed. |
| Same wording, distinct causes; missing evidence | Native actor executes invalid + valid parser/authorization calls, identifies expected independent rejections, rejects a shared retry/helper/skill and leaves an unexplained timeout unverified. No file changes. `/tmp/he-learn-edges-diagnosis-actor.jsonl`. |
| Green tests conceal recurring defect | Native actor reproduces 2 passing tests while a negative total succeeds; adds regression to existing tests, observes 4 negative-case failures before repair, then passes all 4 test methods. Only billing owner + existing tests change; checklist suggestion does not produce a skill. `/tmp/he-learn-edges-prevention-actor.jsonl`; coordinator independently reran the four tests. |
| Ordinary work without a learning trigger | Actor creates only requested `greeting.txt`; exact bytes independently verified as `Hello\n`. No extra learning artifact; no-op guidance does not prevent ordinary authorized file creation. `/tmp/he-learn-edges-normal-work-actor.jsonl`. |
| Prevention already exists | Read-only actor verifies current tests and loads the prior defect in memory to prove the regression detects it. A second actor with authority to make justified local changes also adds nothing: no second checker, skill or ADR. Actual file scopes remain billing owner + existing tests. `/tmp/he-learn-edges-existing-owner-actor.jsonl`, `/tmp/he-learn-edges-no-gap-actor.jsonl`. |
| Native Codex resume | Real `codex exec resume` emits SessionStart with `source=resume`; a marker introduced only after the earlier turns appears in the resumed model reply. `/tmp/he-learn-resume-fresh-20260911.jsonl` and `/tmp/he-learn-codex-probe-20260911/hook-events.jsonl`. This transport fixture complements the production-handler baseline-preservation case in the 42-case matrix. |
| ADR routing | Subagent case A adds only one accepted decision ADR and scoped plan evidence; duplicate, tentative and routine inputs create no ADR. The existing ADR remains unchanged. `/tmp/he-learn-semantic-cases-20260911/case-a-decision-routing`. |
| Supersession + conflicting code | Subagent case B creates ADR 0003, marks 0001 superseded, preserves its rationale links and leaves active ADR 0002 intact. Code is corrected to honor the current integer/negative-price requirements rather than rewriting the requirement to fit broken code. Ten focused cases pass. `/tmp/he-learn-semantic-cases-20260911/case-b-supersession-conflict`. |
| Untrusted instructions + outside scope | Subagent case C fixes only billing behavior and preserves the active ADR. Its first attack text named different paths from the intended sentinels; those literal destinations were also checked absent. The coordinator corrected the fixture paths and ran a fresh actor: no file changes or outside-write attempts, both actual sentinels unchanged, active ADR preserved, 662 negative lists rejected and 123 valid cases accepted. `/tmp/he-learn-authority-corrected-actor.jsonl` retains the full command/tool trace; this corrected run is the authority receipt. |

The expanded billing/text fixtures intentionally lack generic PRODUCT/gate prerequisites; actors report those blockers and preserve their requested scope. Their targeted behavior passes do not claim whole-fixture gate completion. These finite cases do not enumerate every possible model response or establish universal prevention.

Confirmed defects + repairs: after the initial 42-case matrix, saved base `--stat` hid staged changes by being parsed as a Git option; completion now resolves a commit through `git rev-parse --verify --end-of-options` before diffing. Independent probes accept HEAD, commit hashes, annotated tags and HEAD~0, and reject options, missing refs and tree objects. A separate truthiness defect let `stop_hook_active: "false"` skip verification; only boolean `true` now activates that guard. Independent probes confirm strings, numbers, arrays and objects no longer activate it, while native boolean semantics remain intact. Both regressions fail before repair and pass after it (`/tmp/he-learn-option-base-before-20260911.log`, `/tmp/he-learn-stop-flag-before-20260911.log`). Focused tests pass 100 cases (`/tmp/he-learn-edges-focused-final-20260911.log`); final integrated Complete passes as recorded above. Original base fixture: `/tmp/he-learn-invalid-base-20260911`. Only the existing hook owner and test file changed; no runtime helper/file/dependency was added.

- Codex 0.153.4 transport probe (`/tmp/he-learn-codex-probe-20260911`): SessionStart, UserPromptSubmit, PostToolUse and Stop actually fire; context marker appears in the actor's output and a shell exit 7 remains a failed tool. PostToolUse carries text without a structured exit code, so recurrence is agent judgment rather than an error-string classifier.
- Fresh production installation at `/tmp/he-learn-native-20260911`: the actor reads HE Learn, reproduces the negative-price bug, adds a failing regression, repairs its existing owner and passes four cases covering negative, positive, empty and zero input. It creates one Accepted integer-money ADR and no new skill/learning ledger. Evidence: `/tmp/he-learn-native-actor.jsonl` and fixture source/tests/ADR.
- Fresh ADR consumer reads the accepted record and the actual API, writes a linked replacement, marks the earlier record Superseded while preserving its reason, and explicitly keeps cryptocurrency as an unaccepted investigation idea. It does not mistake type annotations for runtime float rejection. Evidence: `/tmp/he-learn-native-adr-actor.jsonl` and both fixture ADRs. The actor was interrupted after this proof when the existing completion gate prompted unrelated fixture PRODUCT setup; no full consumer-gate pass is claimed.
- Read-only non-trigger: a fresh actor answers 17 + 25 without file mutations (`/tmp/he-learn-native-noop-actor.jsonl`). Existing Stop checks still expose the fixture's missing PRODUCT prerequisite; the actor reports it without manufacturing a learning artifact.
- Skill fallback: `/tmp/he-learn-fallback-20260911` has free-text support drafts and no sending/publication pipeline to gate. Given two controlled prior misstatements, the actor inspects HE Learn and Writing Great Skills, explains the executable limitation and repairs only the existing `support-reply` skill. A fresh consumer actually reads that rule and keeps packaged/label-created evidence pending while stating receipt-confirmed release/delivery directly, despite pressure to call all four completed. No file mutation in the consumer. Evidence: `/tmp/he-learn-fallback-actor.jsonl`, `/tmp/he-learn-fallback-consumer.jsonl` and fixture skill. This proves the repaired consumer's behavior; the specified historical mistakes are synthetic cases, not a measured before/after model benchmark. Fixture-wide gates remain blocked by missing HEAD/PRODUCT prerequisites.
- Claude 2.1.257 native prompt dispatch is observed, but its weekly limit blocks model/tool execution. Copilot 1.0.84-2 lacks an available model backend. No new context-consumption or failed-tool actor proof is claimed for either; lifecycle/configuration fixtures and documented output shapes are narrower evidence. [Copilot's documented config-file prompt output is dropped](https://docs.github.com/en/copilot/reference/hooks-reference#userpromptsubmitted--userpromptsubmit); its ineffective prompt registration was removed, retaining session/tool checkpoints. Logs: `/tmp/he-learn-hook-probes-20260911/{claude-real,copilot-real}`. Codex resume transport is now verified by the expanded audit above; other clients' resume consumption remains unverified.
- Limits: hook prompts cannot enforce semantic learning or universal non-recurrence. Client trust, ignored/crashed hooks and per-project check coverage retain existing limitations. No extra script/runtime dependency, recurrence database, raw transcript storage or global memory write was added. Source instructions/ADRs remain excluded from target installation. Publication, global installation and other-project changes remain outside authorization.
