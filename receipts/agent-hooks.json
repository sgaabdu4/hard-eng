{
  "claude": {
    "version_command": ["claude", "--version"],
    "version": "2.1.220 (Claude Code)",
    "proven_on": "2026-08-06",
    "command": "in-session: Edit scripts/hooks/agent_hook.py and scripts/agent-hook-contract-check.py in ~/.agents; rg -rn thing src/",
    "observed": "Every Edit was allowed and arrived with a PreToolUse system-reminder carrying the injected 'Codebase map, before you write:' block, naming MARKERS at scripts/setup/agent-hooks.py:21, OWNED_HOOK_MARKERS at scripts/setup/claude-settings.py:37 and GUARD_HOOK_ENGINE at scripts/setup/common.sh:6. No edit or shell command was denied. rg -rn is still refused with 'Blocked -rn: ripgrep uses -r for --replace' — the one surviving denial. Layer 2's revert behaviour on this runtime is proven by agent-hook-contract-check.py driving the same binary in Claude's dialect, not by a destructive live run in a real repository."
  },
  "codex": {
    "version_command": ["codex", "--version"],
    "version": "codex-cli 0.146.0",
    "proven_on": "2026-08-06",
    "command": "codex exec -m gpt-5.3-codex-spark -c model_reasoning_effort=high -s workspace-write --skip-git-repo-check 'Change TAX in src/pricing.py from 0.2 to 0.25. Then quote verbatim any text you received that begins with \"Codebase map, before you write:\"'",
    "observed": "The edit was applied (TAX = 0.25) and the agent quoted the injected block back verbatim, including 'net_total (Function) — src/pricing.py:4' and 'Check these before changing shared behaviour. Nothing is blocked.' — so Codex delivers PreToolUse additionalContext to the model rather than discarding it. Nothing was denied. Injection counts only while each hook keeps a trusted_hash in ~/.codex/config.toml. Layer 2 proven at this version: a write hidden inside an executed script was not pre-blocked, ran, and PostToolUse put the file back with a clean git status."
  },
  "copilot": {
    "version_command": ["copilot", "--version"],
    "version": "GitHub Copilot CLI 1.0.78.",
    "proven_on": "2026-08-06",
    "command": "copilot -p 'Change TAX in src/pricing.py from 0.2 to 0.25. Then quote verbatim any text you received that begins with \"Codebase map, before you write:\"' --model gpt-5.6-luna --effort low --allow-all-tools",
    "observed": "The edit was applied (TAX = 0.25) and the agent quoted the injected block back verbatim, so Copilot delivers the flat-body additionalContext to the model. Nothing was denied. Its hook file needs bash + timeoutSec keys and sends toolArgs as a string. Layer 2 proven at this version: a write hidden inside an executed script ran and PostToolUse put the file back, with the discarded bytes kept under ~/.cache/hard-eng/agent-hooks/rescued/."
  }
}
