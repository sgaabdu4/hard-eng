{
  "evidence": [
    "GitHub Actions run 31936908439 failed Ubuntu ARM64 Fish path convergence, Ubuntu x64 shared-scan rendezvous, and macOS ARM64 crash-timeout setup.",
    "GitHub Actions run 31938211265 proved the first correction still over-constrained Fish platform PATH entries and measured a different React Doctor startup path from the timed crash path."
  ],
  "failure": "Local gates passed while the first cross-platform Hard Eng CI run failed three required jobs in setup and source-tree coordination fixtures.",
  "helper": {
    "name": "he-learn",
    "selections": 1,
    "state": "launched"
  },
  "learning_id": "ci-fixture-portability",
  "next_action": "none",
  "occurrences": 6,
  "prevention": {
    "kind": "deterministic",
    "owner": ".github/workflows/check-skill-contracts.yml",
    "proof": "scripts/check-skill-contracts.py",
    "valid_fixture": "skills/deterministic-checks/scripts/source_tree_coordination_regression_check.py",
    "violation_fixture": "scripts/setup-path-fish-contract-check.py"
  },
  "root_cause": "Repository test fixtures asserted unrelated platform PATH bytes and selected a crash deadline from a different startup path, so local speed and configuration hid failures on supported CI hosts.",
  "schema_version": 1,
  "status": "resolved",
  "trigger": "false-passing-check"
}
