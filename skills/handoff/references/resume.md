# Resume

1. Read `HANDOFF.md` fully before any action.
2. Read every `Pointers` artifact relevant to `Next` step 1; pointer target missing → treat as drift.
3. Verify against current state = directory + branch + commit + `git status` + listed files exist; drift → report exact delta before acting.
4. Re-verify every `Open` assumption; failed assumption → surface; silent proceed forbidden.
5. Report = short verified state summary + planned first action; then execute `Next` step 1.
6. Material conflict between handoff and current state/user request → ask; handoff never overrides the user.
7. Cleanup = none; next handoff write overwrites the file.
