# Implement Workflow

Use `/tdd` where possible at pre-agreed seams.

Run typechecking and focused tests during implementation, then run the full test suite once at the end.

Use `/code-review` after implementation.

Prepare a commit summary. Commit only when the user explicitly asks.
