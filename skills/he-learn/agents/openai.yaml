interface:
  display_name: "Hard Eng Learn"
  short_description: "Prevent proven repository process failures"
  default_prompt: "Use he-learn in the affected repository. Prefer deterministic prevention and create a repository-local skill only for a proven repeated root that cannot be prevented deterministically."
policy:
  allow_implicit_invocation: true
