interface:
  display_name: "Repeated Failure Learning"
  short_description: "Prove recurring failures share one root cause"
  default_prompt: "Use $repeated-failure-learning to prove whether these failures share one root cause."
