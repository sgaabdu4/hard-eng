# Direct Questioning

1. Convert request → bounded decision inventory; exclude objective facts evidence can settle.
2. Apply `SKILL.md` Admission → classify each item + resolve objective gaps/contradictions.
3. Select earliest user decision → render `SKILL.md` Question → record only the confirmed answer.
4. Recompute inventory after every answer/correction → repeat until `SKILL.md` Return gate passes.
