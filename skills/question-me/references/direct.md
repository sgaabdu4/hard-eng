# Direct Questioning

1. Convert request → bounded decision inventory; exclude objective facts evidence can settle.
2. Apply `SKILL.md` Admission → classify each item + resolve objective gaps/contradictions.
3. Select next dependency frontier by impact → batch every independent material decision → render one `SKILL.md` decision set → wait once.
4. Recompute inventory after every answer/correction → repeat by frontier until `SKILL.md` Return gate passes.
