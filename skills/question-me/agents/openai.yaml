interface:
  display_name: "Question Me"
  short_description: "Resolve product ambiguity with evidence"
  default_prompt: "Use $question-me to resolve every material decision without guessing."
policy:
  allow_implicit_invocation: false
