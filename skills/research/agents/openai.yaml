interface:
  display_name: "Research"
  short_description: "Gather current evidence for decisions"
  default_prompt: "Use $research to gather evidence for this decision from primary sources."
policy:
  allow_implicit_invocation: true
