# UI System

1. Locate tokens/theme + primitives + components + layouts/pages + examples/previews; verify actual use.
2. Search by interaction/domain pattern and visual value; identify duplicate/near-duplicate owners.
3. Reuse or deepen the closest owner; create the smallest missing token/component only from proven reuse/composition gap.
4. Place decisions: token/theme = reusable visual value; primitive = raw control; component = composed contract; layout/page = orchestration.
5. Migrate every touched duplicate/hardcoded reusable value to the selected owner; no alias/parallel owner.
6. Use matching stack skill + `$deterministic-checks`; verify states, responsive behavior, accessibility, and affected examples/tests.
7. Report selected owner + migrated consumers + remaining one-off values + proof/gaps.

- New aesthetic/interaction choice without accepted direction → `$he`; do not invent production design.
