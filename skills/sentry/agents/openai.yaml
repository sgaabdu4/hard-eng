interface:
  display_name: "Sentry"
  short_description: "Investigate and resolve Sentry issues"
  default_prompt: "Use $sentry to investigate and fix this Sentry issue through the installed CLI."
policy:
  allow_implicit_invocation: true
