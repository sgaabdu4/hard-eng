interface:
  display_name: "Hard Eng Ship"
  short_description: "Publish one exact green snapshot safely"
  default_prompt: "Use $he-ship after $he routes an exact green snapshot to deliver it through approved Git actions, publish gates, and CI."
policy:
  allow_implicit_invocation: true
