interface:
  display_name: "Deterministic Checks"
  short_description: "Enforce deterministic project quality gates"
  default_prompt: "Use deterministic-checks to run bounded project gates and require compatible real-tool proof before paid or native retries."
