---
name: flutter-workflow
description: Use for Flutter app work only when the repository uses Riverpod/codegen, Freezed, GoRouter, Hive, l10n, or Flutter UI/E2E. Skip BLoC, GetX, Provider/ChangeNotifier, MobX, Redux, and pure-Dart packages without Flutter app context.
---

# Flutter workflow

Read [flutter.md](references/flutter.md) before planning or editing matching
Flutter app code. Project rules and locked versions remain authoritative.

Use the Hard Eng lifecycle only when its route applies. This optional skill
adds Flutter-specific ownership and proof; it never creates another stage,
state file, installer, model call, or background process.
